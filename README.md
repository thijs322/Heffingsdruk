# flask_plotly_template
Template for a small flask + wtforms + plotly app

@thijs je kan de packages installeren met 

`pip install -r requirements.txt`

en het vervolgens runnen met

`python flask_app.py`
